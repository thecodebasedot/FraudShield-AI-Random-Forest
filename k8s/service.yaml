apiVersion: v1
kind: Service
metadata:
  name: fraudshield-api
  labels:
    app: fraudshield-api
spec:
  type: ClusterIP
  selector:
    app: fraudshield-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
